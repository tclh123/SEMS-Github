@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("登出", "Logout", "Account", new { area = "Admin" }, null) ]</text>
}
else {
    @:[ @Html.ActionLink("返回", "Index", "Home", new { area = "" }, null) ]
}